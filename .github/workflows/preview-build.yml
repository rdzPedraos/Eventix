name: Preview build

on:
  pull_request:
    types: [opened, synchronize, reopened]

concurrency:
  group: preview-build-pr-${{ github.event.pull_request.number }}
  cancel-in-progress: true

permissions:
  pull-requests: write
  contents: read
  statuses: write

env:
  PREVIEW_LABEL: preview-ready

jobs:
  unlabel-and-clean:
    runs-on: ubuntu-latest
    steps:
      - name: Reset PR state
        uses: actions/github-script@v8
        env:
          PREVIEW_LABEL: ${{ env.PREVIEW_LABEL }}
        with:
          script: |
            try {
              await github.rest.issues.removeLabel({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                name: process.env.PREVIEW_LABEL,
              });
            } catch (error) {
              if (error.status !== 404) throw error;
            }

  build-and-push:
    needs: unlabel-and-clean
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - uses: docker/build-push-action@v6
        with:
          context: .
          file: deploy/Dockerfile
          push: true
          tags: rdzpedraos/eventix:pr-${{ github.event.pull_request.number }}

  signal-deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - name: Mark PR ready for preview
        uses: actions/github-script@v8
        env:
          PREVIEW_LABEL: ${{ env.PREVIEW_LABEL }}
        with:
          script: |
            const sha = context.payload.pull_request.head.sha;

            await github.rest.repos.createCommitStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              sha,
              state: "pending",
              context: "argo/preview",
              description: "Despliegue de aplicación en construcción…",
            });

            await github.rest.issues.addLabels({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              labels: [process.env.PREVIEW_LABEL],
            });
