apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: eventix-previews
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - pullRequest:
        requeueAfterSeconds: 30
        github:
          owner: rdzPedraos
          repo: Eventix
          appSecretName: argocd-notifications-secret
          labels:
            - preview-ready
  template:
    metadata:
      name: "eventix-pr-{{ .number }}"
      annotations:
        preview-url: "http://pr-{{ .number }}.eventix.k8s.test"
        notifications.argoproj.io/subscribe.on-sync-running.github: ""
        notifications.argoproj.io/subscribe.on-deployed.github: ""
        notifications.argoproj.io/subscribe.on-sync-failed.github: ""
        notifications.argoproj.io/subscribe.on-health-degraded.github: ""
    spec:
      project: default
      source:
        repoURL: https://github.com/rdzPedraos/Eventix.git
        targetRevision: "{{ .head_sha }}"
        path: deploy/helm
        helm:
          releaseName: eventix
          valueFiles:
            - values.yaml
            - values-preview.yaml
          parameters:
            - name: domain
              value: "pr-{{ .number }}.eventix.k8s.test"
            - name: image.tag
              value: "{{ .head_sha }}"
      destination:
        server: https://kubernetes.default.svc
        namespace: "eventix-pr-{{ .number }}"
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        retry:
          limit: 3
          refresh: true
        syncOptions:
          - CreateNamespace=true
